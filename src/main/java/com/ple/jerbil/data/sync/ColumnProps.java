package com.ple.jerbil.data.sync;

public enum ColumnProps implements Props {
  name,
  datatype,
  values,
  size,
  precision,
  generatedFrom,
  index,
  primary,
  foreign,
  fulltext,
  unique,
  nullable,
  defaultVal,
}
