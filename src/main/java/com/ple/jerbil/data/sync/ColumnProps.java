package com.ple.jerbil.data.sync;

public enum ColumnProps implements Props {
  exists,
  name,
  datatype,
  enumValues,
  setValues,
  size,
  precision,
  generatedFrom,
  index,
  primary,
  foreign,
  fulltext,
  unique,
  nullable,
  defaultVal,
}
